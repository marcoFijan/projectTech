<!-- INCLUDE HEAD -->
<%- include('components/head.ejs') -%>

<body id="homepage">
  <!-- INCLUDE HEADER -->
  <%- include('components/header.ejs') -%>

  <!-- INCLUDE ASIDE -->
  <%- include('components/aside.ejs') -%>
  <main>
    <!-- ARTICLE WITH INFORMATION ABOUT THE WEBSITE -->
    <article>
      <h1>Welkom bij Geek Matching!</h1>
      <p>Om het makkelijk te maken, hebben wij alle gebruikers die 1 of meerdere interesses met je delen alvast hieronder gezet. Bezoek gerust de pagina's van die profielen. Wie weet waar het naar toe kan gaan...</p>
      <p>Om de website een veilige website te maken voor iedereen, hebben we een paar huishoudelijke regels opgezet. Vergeet niet om deze regels aandachtig door te lezen aangezien wij genoodzaakt kunnen zijn om profielen die deze regels overtreden te bannen van deze website</p>
      <a href="/rules">Regels</a>
    </article>
    <!-- LIST OF ALL PROFILES WITH MATCHING INTERESTS -->
      <section>
        <h1>Profielen met dezelfde interesses</h1>
        <ul>
          <% for (var i = 0; i < profiles.length; i++) { %>
            <a href="/profile/<%= profiles[i]._id %>">
              <li>
                <img src="/static/images/profileImage.svg">
                <h2><%= profiles[i].name %>, <%= profiles[i].age %></h2>
                <p><%= profiles[i].location %></p>
              </li>
            </a>
          <% } %>
        </ul>
      </section>
      <!-- LIST OF ALL ONLINE PROFILES -->
      <section>
        <h1>Online profielen</h1>
        <ul>
          <% for (var i = 0; i < onlineProfiles.length; i++) { %>
            <a href="/profile/<%= onlineProfiles[i]._id %>">
              <li class="onlineProfile">
                <img src="/static/images/profileImage.svg">
                <h2><%= onlineProfiles[i].name %>, <%= onlineProfiles[i].age %></h2>
                <p><%= onlineProfiles[i].location %></p>
              </li>
            </a>
          <% } %>
        </ul>
      </section>
      <!-- LIST OF ALL PROFILES -->
      <section>
        <h1>Alle profielen</h1>
        <ul>
          <% for (var i = 0; i < allProfiles.length; i++) { %>
            <a href="/profile/<%= allProfiles[i]._id %>">
              <li>
                <img src="/static/images/profileImage.svg">
                <h2><%= allProfiles[i].name %>, <%= allProfiles[i].age %></h2>
                <p><%= allProfiles[i].location %></p>
              </li>
            </a>
          <% } %>
        </ul>
      </section>
      <!-- INCLUDE ASIDE -->
      <%- include('components/footer.ejs') -%>
  </main>
</body>
</html>
